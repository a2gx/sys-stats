services:

  daemon:
    container_name: app_daemon
    build:
      context: .
      dockerfile: build/Dockerfile_daemon
    ports:
      - "50055:50055"
    volumes:
      - ./configs:/etc/sys-stats:ro
